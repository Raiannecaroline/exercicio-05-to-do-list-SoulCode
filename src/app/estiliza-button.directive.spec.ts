import { EstilizaButtonDirective } from './estiliza-button.directive';

describe('EstilizaButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new EstilizaButtonDirective();
    expect(directive).toBeTruthy();
  });
});
